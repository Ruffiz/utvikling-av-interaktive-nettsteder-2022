//import MyComponent from "./MyComponent";

const Title = ({ title }) => {
  return (
    <>
      <h1>{(title = "It Works")}</h1>
      {/* <p>{title}</p>
      <p>{myOtherTitle}</p> */}
    </>
  );
};

export default Title;
